import 'package:flutter/material.dart';

class Corridors extends StatelessWidget {
  const Corridors({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: (
        Text("Corridors screen", style: Theme.of(context).textTheme.headlineSmall,)
      ),
    );
  }
}